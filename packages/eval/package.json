{
  "name": "@engram/eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Engram eval harness: retrieval + consolidation metrics, before/after-sleep report.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "start": "tsx src/run.ts"
  },
  "dependencies": {
    "@engram/memory": "workspace:*",
    "@engram/shared": "workspace:*"
  }
}
